/// A student linked to the signed-in parent for the active school.
class ParentLinkedChild {
  const ParentLinkedChild({
    required this.id,
    required this.displayName,
  });

  final String id;
  final String displayName;
}
